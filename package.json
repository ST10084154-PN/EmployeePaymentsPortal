
{
  "name": "employee-payments-portal",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npm run start --prefix server",
    "client": "npm start --prefix client",
    "server": "npm start --prefix server",
    "test": "npm test --prefix server"
  },
  "engines": {
    "node": ">=18"
  }
}
